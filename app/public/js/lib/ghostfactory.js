var GhostFactory = function(tileSize) {
  this.tileSize = tileSize;
};

GhostFactory.prototype = {
  new: function(name, image, gridX, gridY, tileSize) {
    switch(name){
    case 'Bertie':

      break;
    }
  }
};
